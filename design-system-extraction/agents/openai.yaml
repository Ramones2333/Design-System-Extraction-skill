interface:
  display_name: "Design System Extraction | 设计系统提炼"
  short_description: "Bilingual extraction of design systems and specs"
  default_prompt: "Use $design-system-extraction to reverse-engineer a reusable design system, design tokens, component specs, page templates, and standardization guidance from these Figma files, screens, or component libraries."

policy:
  allow_implicit_invocation: true
